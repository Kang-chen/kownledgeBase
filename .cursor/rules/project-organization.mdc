---
alwaysApply: true
description: "Generic rules for project-based organization and rule loading guidance."
---

# Project Organization Rules

## Project Memory System

项目元信息存储在 `knowledge/projects/` 目录下，作为 Agent 的"记忆"。

### Memory 加载

执行归档任务时，**必须首先读取**:
```
knowledge/projects/_index.md
```

此索引文件包含：
- 活跃项目列表
- 项目关键词映射（用于自动识别文件归属）
- 项目相关目录结构

## Project-Based File Structure

```
knowledge/
├── projects/                    # 项目元信息 (Agent Memory)
│   ├── _index.md               # 项目索引（必读）
│   ├── {project-a}.md          # 项目A详细信息
│   └── {project-b}.md          # 项目B详细信息
├── programming/
│   ├── {project-a}/            # 项目A - 代码相关
│   ├── {project-b}/            # 项目B - 代码相关
│   └── commands/               # 通用命令（无项目归属）
├── biology/
│   ├── {project-a}/            # 项目A - 生物学相关
│   └── {project-b}/            # 项目B - 生物学相关
└── planning/
    └── (按日期/项目组织 TODO)
```

## Rule Loading Guidance (按需加载指南)

### 强制加载规则

当执行以下操作时，**必须主动使用 Read 工具读取**对应的规则文件：

| 操作类型 | 必须加载的规则 |
|---------|---------------|
| 归档 inbox / 组织文件 | `knowledge/projects/_index.md` (项目索引) |
| 创建/编辑 planning 文件 | `.cursor/rules/planning_notes.mdc` |
| 创建/编辑 programming 文件 | `.cursor/rules/programming_notes.mdc` |
| 创建/编辑 biology 文件 | `.cursor/rules/biology_notes.mdc` |
| 处理敏感信息 | `.cursor/rules/private-info-rule.mdc` |
| 更新 changelog | `.cursor/rules/changelog_rules.mdc` |

### 为什么需要主动加载？

Cursor 的规则加载机制：
- `alwaysApply: true` → 始终加载
- `alwaysApply: false` + `globs` → **仅在打开匹配文件时加载**

因此，当处理 inbox 文件时，目标目录的规则（如 `planning_notes.mdc`）不会自动加载，Agent 必须主动读取。

## Inbox Archival Workflow

1. **加载项目索引**: `Read: knowledge/projects/_index.md`
2. **分析文件内容**: 识别主题和关键词
3. **匹配项目**: 根据关键词映射确定项目归属
4. **加载目标规则**: 根据目标目录读取对应规则文件
5. **确定目标路径**: `{category}/{project}/` 或 `{category}/`
6. **执行归档**: 移动文件、更新 frontmatter、记录 changelog
7. **更新项目索引**: 如有新项目，更新 `_index.md`

## Subdirectory Organization

为防止单层目录文件过多：
- **阈值**: 同类文件超过 5 个时创建子目录
- **命名**: 小写英文，按项目/主题命名
- **资源文件**: `.assets` 文件夹与对应 `.md` 文件同目录
